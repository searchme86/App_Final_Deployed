import React from 'react';

import CategorySelect from './CategorySelect';

function CategoryView({ handleState }) {
  return <CategorySelect handleState={handleState} />;
}

export default CategoryView;
