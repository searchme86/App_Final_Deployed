import styled from 'styled-components';

export const NotFoundContainer = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;

export const NotFoundTitle = styled.strong`
  display: block;
  font-size: 120px;
  line-height: 1;
`;

export const NotFoundTitleDes = styled.p`
  text-align: center;
  font-size: 20px;
  margin: 10px 0 0 0;
`;
